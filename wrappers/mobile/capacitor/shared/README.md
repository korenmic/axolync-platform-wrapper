# Capacitor Shared

Reserved for Capacitor-wide bridge, defaults, and native companion host utilities that are not Android-only.

Generic native service companion protocol, deployment, and diagnostics authority lives under `native-service-companions/`. The folder exists now to stop new shared wrapper logic from being added under Android-only paths while the physical Android move is still in compatibility mode.
