# Capacitor Wrapper Family

Capacitor owns mobile webview host behavior under this wrapper authority.

- `android/` documents and will own the Android Capacitor host.
- `shared/` is reserved for Capacitor-wide bridge/default/deployment code.
- `ios/` is a placeholder only. It does not claim iOS artifact support.
