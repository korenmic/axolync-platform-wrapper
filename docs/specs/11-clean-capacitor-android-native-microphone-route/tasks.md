# Tasks: Clean Capacitor Android Native Microphone Route

- [x] 1. Add the clean Capacitor Android native microphone bridge methods and staged JavaScript host bridge wiring.
- [x] 2. Implement a single-source `UNPROCESSED` Android `AudioRecord` capture session with compact chunk diagnostics.
- [x] 3. Remove Android Auto/car-mode detection, cached car-state policy, playback-capture diagnostics, and audio-source fallback logic from this clean route.
- [x] 4. Add tests proving the clean bridge uses the Capacitor global listener and only the `UNPROCESSED` native source.
